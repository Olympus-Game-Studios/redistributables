# Scripting Layer API Reference

## Core Classes

### ScriptBase

Base class for all game scripts. Inherit from this to create custom scripts.

#### Constructor
```cpp
// Your derived class constructor
MyScript(Entity entity, Scene* scene) {
    m_Entity = entity;
    m_Scene = scene;
}
```

... (archived full content)
