# Asset Browser - Create Lua Script Feature

## Overview

A new context menu option has been added to the asset browser that allows developers to quickly create new Lua script files directly from the project browser. Simply right-click on any project and select "New Lua Script..." to generate a template `.lua` file.

... (archived full content)
